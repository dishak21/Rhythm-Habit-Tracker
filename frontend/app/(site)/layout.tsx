import type { Metadata } from "next";
import "@/app/globals.css";
import AnimatedNavBar from "@/components/ui/AnimatedNavBar";

export const metadata: Metadata = {
  title: "Rhythm",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <div
      id="bg_color_container"
      className="w-full relative min-h-screen overflow-auto duration-500"
      style={{
        backgroundImage: "url('/img/bg_habits.jpg')",
        backgroundSize: "cover",
        backgroundPosition: "center",
        backgroundAttachment: "fixed", // Optional for a fixed background effect
        backgroundBlendMode: "overlay", // Adds mix-blend-overlay effect

        backgroundColor: "#333", // Adds a background color for blending
      }}
    >
      <div className=" h-full max-w-7xl mx-auto flex w-full items-center transition-colors">
        <div className="w-full mx-auto">
          <AnimatedNavBar />
          <div className="mx-1 mt-3">{children}</div>
        </div>
      </div>
    </div>
  );
}
